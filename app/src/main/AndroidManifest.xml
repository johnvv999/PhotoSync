<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" /> <!-- Android 13+ granular media permission -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" /> <!-- Android 12 and below -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- Needed by GoogleAccountCredential to resolve the signed-in account via
         AccountManager when minting Drive API tokens -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <application
        android:allowBackup="true"
        android:label="PhotoSync"
        android:theme="@style/Theme.Material3.DayNight"
        android:icon="@mipmap/photosynclauncher"
        android:roundIcon="@mipmap/photosynclauncher_round">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PhotoSync"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SyncControlActivity"
            android:exported="false"
            android:label="@string/sync_options"
            android:theme="@style/Theme.PhotoSync" />
        <activity
            android:name=".SyncedPhotosActivity"
            android:exported="false"
            android:label="@string/browse_synced_photos"
            android:theme="@style/Theme.PhotoSync" />
        <activity
            android:name=".MapViewActivity"
            android:exported="false"
            android:label="@string/map_link"
            android:theme="@style/Theme.PhotoSync" />
        <activity
            android:name=".FullScreenPhotoActivity"
            android:exported="false"
            android:theme="@style/Theme.PhotoSync" />
    </application>

</manifest>